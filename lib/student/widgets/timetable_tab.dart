// lib/student/widgets/timetable_tab.dart
import 'package:flutter/material.dart';

class TimetableTab extends StatelessWidget {
  const TimetableTab({super.key});
  @override
  Widget build(BuildContext context) {
    // Placeholder UI for the timetable
    return const Center(child: Text('Scheduled Classes & Timetable will appear here.'));
  }
}