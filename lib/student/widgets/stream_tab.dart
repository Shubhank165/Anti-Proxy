// lib/student/widgets/stream_tab.dart
import 'package:flutter/material.dart';

class StreamTab extends StatelessWidget {
  const StreamTab({super.key});
  @override
  Widget build(BuildContext context) {
    // Placeholder UI for the stream. You can use a ListView for posts.
    return const Center(child: Text('Course Stream - Announcements will appear here.'));
  }
}
